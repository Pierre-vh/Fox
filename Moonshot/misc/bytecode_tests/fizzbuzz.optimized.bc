	declfunc 0					// 0 is the function index in the globals table
		.meta	"fizzbuzz"		// .meta tag for the function : "fizzbuzz"
		decllocal	0		
			.meta	"count" 
		loadloc	0 // while part
		const	100
		cmp_leq		// TOS2 less or equal (<=) TOS1
		cond_jump	56 // Jump
		loadloc 0 // while body: first if
		const 3
		mod
		const	0
		cmp_eq
		cond_jump 4	// goes to 13+2 = 15 if false
		loadext 0	// load external function 0 (print)
		const "fizz"
		callf 1	// calls a function with 1 arg. 1 arg is pop'd + the func ref (loadext)
		loadloc 0 // while body : second if
		const 3
		mod
		const 0
		cmp_eq
		cond_jump 4
		loadext 0
		const "buzz"
		callf 1
		loadloc 0 // while body :third if
		const 3
		mod
		const 0	
		cmp_eq
		loadloc 0
		const 5
		mod
		const 0
		cmp_eq
		and
		cond_jump 4
		loadext 0
		const "fizzbuzz"
		callf 1
		incrloc 0	// increments local 0
		goto 6 // end of loop
		returnv	// return from function, pop frame, add return value to main stack (here, none, so nothing is added)
				// returnv returns void.