func fizzbuzz() 
{
	let count : int;
	while(count <= 100)
	{
		if(count % 3 == 0)
			print("Fizz");
		if(count % 5 == 0)
			print("Buzz");
		if((count % 3 == 0) && (count % 5 ==0))
			print(count);
		count = count+1;
	}
}

Bytecode translation example: see fizzbuzz.bc
