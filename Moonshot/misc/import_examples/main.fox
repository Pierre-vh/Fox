import Fox.*; // import everything from the namespace Fox.std (standard library)

func main() : int
{
	Fox.io.print("Hello, world!");
	let x = Fox.io.readLine(); // Read a line (string)
	
	let flt_res : float;
	if(x.isFloat())	// This isn't a function call, it's a expr_func call on a string!
	{
		fl_res = x.toFloat();
	}
	else 
	{
		Fox.io.print("Wrong input.");
	}
}