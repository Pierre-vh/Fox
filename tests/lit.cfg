import os
import sys
import lit
import lit.formats

config.name = 'Moonshot'
config.suffixes = ['.fox']
config.test_format = lit.formats.ShTest(execute_external=False)
config.test_source_root = os.path.dirname(__file__)

execPath = lit_config.params.get('moonshot_dir', None)
if execPath:
  print(execPath)
else:
  print("Fox executable Not found")