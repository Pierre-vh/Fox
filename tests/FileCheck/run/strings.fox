// RUN: %fox-run | %filecheck

func main() {
  stringInterfaceTest();
}

func stringInterfaceTest() {
  let str : string = "おはよう。";
  // numBytes counts the null terminator
  // CHECK: num bytes: 15
  printString("num bytes: " + $numBytes(str) + "\n"); 
  // CHECK-NEXT: length: 5
  printString("length: " + $length(str) + "\n");
}