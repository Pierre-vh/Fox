// RUN: %fox-verify

// Theses tests check for errors in more advanced expressions

// Basic expressions only (no variables)
func basic_expr_chck() {
  ((((3+2)/46) == 0) && (3+4-4) as bool) as double == 0.0;
  ((((11-6/2)*9-10%3)/26-23)/(24.0+2.34/3.14) as int) as double;
  // TODO: Add more of theses
}