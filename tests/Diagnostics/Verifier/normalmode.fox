// RUN: %fox %s -verify
// XFAIL:*

// This test checks that the "normal" mode works. 
// It is supposed to fail because in normal mode we check
// that all expected diagnostics were emitted.

let x : int; // expect-error: Expected a ';'
